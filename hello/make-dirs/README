The make file in this directory builds the java code and places it in an archive using multiple sub-dirs.

src		(the source directory for the java code)
build 		(the class directory)
dist		(the jar direcotry)

make          (defaults to all)
make compile  (compiles the code)
make jar      (builds the java archive)
make run      (runs the code)
make clean    (cleans up the directory)
